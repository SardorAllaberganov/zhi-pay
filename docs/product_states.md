# Product States — ZhiPay

> Snapshot of build progress per surface across the two scopes (admin dashboard + mobile app).
> Source: [`ai_context/AI_CONTEXT.md`](../ai_context/AI_CONTEXT.md) + [`dashboard/src/router.tsx`](../dashboard/src/router.tsx).
> Last updated: 2026-05-04 (Phase 29 — **Working-files frames refactored to component instances + 2 missing Onboarding slides added + 30 inline icons swapped to icon instances + Onboarding illustrations promoted to component set + Icon / Bell authored and 2 inline bells swapped + Icon / Inbox authored for empty-state usage**. All 17 reference screens on `⏰ Working files` (`1:5`) had their inline geometry replaced with instances from `❖ Components` wherever a library counterpart existed: Auth top bar (Step=1|2|3|4) · Brand/ZhiMark · Button (Primary/Secondary Lg) · Input/Field (Phone/Search/Text) · Input/Otp (Length=Six) · Input/NumberPad (with `AmountField.visible=false` override on OTP/PIN/Lock) · Tab bar (Home/More) · Banner (Info) · Avatar/Initials Md · Scheme logo Sm. 2 new component sets authored on `❖ Components` to fill gaps: **Auth top bar** (`170:265`, 4 variants Step=1|2|3|4) and **Brand/ZhiMark** (`170:223`). Components count on `❖ Components` page: **20 → 22 component sets** + 19 lucide icons. **Frame count on `⏰ Working files`: 23 → 25** — `OnboardingScreen` defines 3 slides in code ([`mobile/ZhiPay/auth-screens.jsx`](../mobile/ZhiPay/auth-screens.jsx) `ONBOARDING_SLIDES`) but only slide 1 was in Figma; **slide 2** ("Aniq kurs" + lock-and-chart illustration, `189:345` at x=3754) and **slide 3** ("Xavfsiz va litsenziyalangan" + shield-and-check illustration, `189:389` at x=4276) added by cloning slide-1 frame and rebuilding the 180×180 illustration vector + page-indicator dot position + title/body copy per slide. iOS chrome stayed inline (size mismatch 402w frames vs 375w Status Bar component). Pattern 1 + Processing skipped.

## Legend

| Symbol | Meaning |
|:---:|---|
| ✅ | **Done** — built, deployed, and verified |
| 🚧 | **In progress** — partially built or actively being worked on |
| ❌ | **Todo** — not started; placeholder route or planned screen |

---

## Dashboard (admin / ops / compliance)

Tech stack: Vite + React 18 + TS + Tailwind + shadcn/ui · Router: HashRouter · Deployed via GitHub Pages.

### Foundation

| Layer | Status | Notes |
|---|:---:|---|
| Design tokens (brand 50→950, slate, semantic, shadcn light/dark) | ✅ | [`globals.css`](../dashboard/src/styles/globals.css) + [`tailwind.config.ts`](../dashboard/tailwind.config.ts) |
| Type scale (13px floor, locked) | ✅ | xs=13 / sm=14 / base=15. See [LESSONS](../ai_context/LESSONS.md) 2026-04-29 + 2026-05-01 |
| Table column-header style (locked) | ✅ | Title Case + `text-sm font-medium text-muted-foreground` everywhere — no uppercase/tracking, no active-sort color differential. See [LESSONS](../ai_context/LESSONS.md) 2026-05-02 |
| Detail-page header convention (locked) | ✅ | Inline (NEVER sticky) · structure: back-link / identity / chips · uniform `<ArrowLeft> Back to <list>` link. See [LESSONS](../ai_context/LESSONS.md) 2026-05-02 |
| Detail-page sticky-bottom action bar (locked) | ✅ | `fixed inset-x-0 bottom-0 md:left-[var(--sidebar-width,4rem)]` · AppShell exposes the live sidebar width as a CSS var. See [LESSONS](../ai_context/LESSONS.md) 2026-05-02 |
| Filter-bar pattern (locked) | ✅ | Carded shell `rounded-md border bg-card px-3 py-3 space-y-3` · search input `type="search" pl-9 h-10 bg-background` (well-in-card) · chip row `flex flex-wrap gap-2`. Applied to all 8 standard list pages (Users / Cards / Recipients / News / AuditLog / Stories / ErrorCodes / Blacklist). Specialized layouts (Transfers / KYC Queue / AML Triage) opt out by design |
| Segmented active-state styling (locked) | ✅ | `bg-card` + `shadow-sm` + brand-tinted text/border (chip triggers) or ring (locale tabs / RTE toolbar). shadcn `<TabsTrigger>` `data-[state=active]:bg-card`. White-on-card pill lifted with shadow — replaces earlier `bg-brand-50` / `bg-brand-100` tints for segmentation surfaces |
| shadcn primitives (25) | ✅ | [`components/ui/`](../dashboard/src/components/ui/) |
| ZhiPay domain primitives (19) | ✅ | StatusBadge · TierBadge · SeverityBadge · Money · MaskedPan · SchemeLogo · StatusTimeline · ErrorCell · KeyboardHint · ReviewQueueRow · DestinationBadge · DateRangePicker · StepperNumberInput · DateTimeInput · LocaleFlag · LocaleTabInputs · LocaleTabTextarea · PhoneMockup · WriteButton (offline-aware Button — Phase 22b) |
| App shell (Sidebar / TopBar / breadcrumbs / CommandPalette / HelpOverlay / ThemeProvider / keyboard shortcuts / `<NotificationsBell>` admin in-app inbox / `<UserMenu>` reads live session) | ✅ | [`components/layout/`](../dashboard/src/components/layout/). **HelpOverlay** caps at `max-h-[85vh]` w/ flex-col + scrollable body so the 95-row shortcuts list reaches every viewport; header pinned at top w/ `border-b`. **`<NotificationsBell>`** = TopBar Popover w/ 3 deterministic mock items (AML escalation → `/operations/aml-triage/aml_01` · FX rate update → `/compliance/audit-log?entity=fx_rate` · card frozen → `/customers/cards/c_01`); each item closes the popover on click via controlled `useState` + `setOpen(false)`; empty-state branch renders "You're all caught up" when items=0; red unread dot only when count > 0; footer link → `/compliance/audit-log`. Distinct from the `/content/notifications` composer surface (outbound pushes to mobile end-users) — naming distinction documented in the file header. **`<UserMenu>`** subscribes to `useSession()` so display-name + email + avatar initials reflect the signed-in admin (initials derived first+last word of `displayName`, falling back to first 2 chars for single-word names); Sign-out item now wires `onClick={() => signOut({ reason: 'user' })}` and the `<AuthGuard>` redirect handles the navigation back to `/sign-in?next=...` |
| Auth machinery (mock auth store · `<AuthGuard>` route wrapper · 30-min idle session-timeout hook · sessionStorage persistence · `useSyncExternalStore` reference-stable snapshot) | ✅ | [`lib/auth.ts`](../dashboard/src/lib/auth.ts) + [`data/mockAdminAuth.ts`](../dashboard/src/data/mockAdminAuth.ts). All non-`/sign-in` routes pass through `<AuthGuard>` which redirects to `/sign-in?next=<encoded path>` when no session exists. Idle timer fires `/sign-in?expired=1&next=...` and renders `<SessionExpiredBanner>`. Session held in a module-level `currentSession` variable that's only rewritten on mutation (`writeSession`) or cross-tab `storage` event — `getSnapshot()` returns the cached reference so React's `useSyncExternalStore` doesn't trip the "should be cached to avoid an infinite loop" guard |
| Mock data layer (transfers · transfer-detail · KYC · AML) | ✅ | [`data/`](../dashboard/src/data/) |
| Master-detail layout pattern (locked) | ✅ | KYC + AML; future review surfaces follow |
| Action-bar layout pattern (locked) | ✅ | Fixed-bottom + 2×2 grid on mobile, in-flow on `lg+` |
| Error & system-state foundation (SystemErrorBoundary · MaintenanceGate · PathAwareAuthGuard · OfflineBanner · `<WriteButton>` offline-aware Button primitive · `mockSystemEvents` forensic store · `useNetworkState` · `lib/maintenanceState` · `lib/offlineActionQueue` queue+replay · `useDraftPreservation` form draft hook) | ✅ | [`components/system/`](../dashboard/src/components/system/) + [`lib/maintenanceState.ts`](../dashboard/src/lib/maintenanceState.ts) + [`hooks/useNetworkState.ts`](../dashboard/src/hooks/useNetworkState.ts). Boundary wraps `<AppRoutes>` so render-time crashes render `<ServerErrorState>` in-shell + log a `page_error` row to `mockSystemEvents` with a generated reference id. `<MaintenanceGate>` sits at top-level (above AuthGuard) so the maintenance state takes over for signed-in and signed-out admins alike; preview routes (`/system/preview/*`) are excluded so designers can eyeball other states with the flag on. `<OfflineBanner>` mounts inside `<AppShell>` between TopBar and `<main>`, slate-toned, persistent until network restores. `mockSystemEvents` is forensic-only (NOT bridged into `mockAuditLog`) — same precedent as Phase 20's `mockAdminLoginAudit` (operational signals stay separate from compliance review). |
| GitHub Pages deploy pipeline | ✅ | [`.github/workflows/deploy.yml`](../.github/workflows/deploy.yml) |

### Surfaces

| Surface | Route | Status | Notes |
|---|---|:---:|---|
| Auth — Sign-in | `/sign-in` | ✅ | Email + password only — no 2FA / OTP on the admin surface (TOTP / SMS-OTP belongs to mobile end-user flow). Forgot-password modal directs to out-of-band reset (compliance posture — no self-service). Session-expired banner via `?expired=1&next=...`. Full-bleed layout outside `<AppShell>` (no sidebar / topbar) · light radial gradient `slate-50→brand-50` / dark `slate-950→brand-950` · 440px centered card on `md+`, full-screen w/ `p-6` on `<md` · ZhiPay logo + wordmark above card · ThemeToggle pinned top-right · footer chip "© 2026 ZhiPay · admin v0.1.0" pinned bottom-center. 2 mock seed accounts in `mockAdminAuth.ts` (`super.admin@zhipay.uz` → `/` · `disabled.admin@zhipay.uz` → AUTH_ACCOUNT_DISABLED) both with shared password `zhipay-demo-2026`. **For demo speed, both fields pre-fill on mount** — email defaults to `super.admin@zhipay.uz`, password defaults to `DEMO_PASSWORD` via the `defaultPassword` prop on `<EmailPasswordStep>`; one Sign-in click lands on `/`. Per-email rate limit (5 failed in 15 min → AUTH_RATE_LIMITED w/ `locked_until = now() + 15min`). Inputs use canonical `h-10` height (matches the project's filter-bar / Select pattern); password input has eye/eye-off show/hide toggle inset on the right. All errors surface-scoped via `admin.sign-in.error.*` keys (intentionally NOT in user-facing `error_codes` §7 per security baseline — generic copy, no field-level reveal). 9 i18n keys (EN-only, matching project i18n stub). Auth audit (`mockAdminLoginAudit`) intentionally NOT bridged into central `mockAuditLog` — central log is reserved for entity-state-change events (transfers / KYC / AML / cards / FX / services / content); auth forensics live separately. Schema cascade in `docs/models.md` §10 (`admin_users` + `admin_sessions` + `admin_login_audit` tables + 2 enums + 7 indexes); flow + state-machine docs at [`admin_signin_flow.md`](./mermaid_schemas/admin_signin_flow.md) + [`admin_session_state_machine.md`](./mermaid_schemas/admin_session_state_machine.md); PRD §6.1 row 14 + Gherkin AC fragment. **`<AuthGuard>` wraps every other route** — redirects unauthenticated requests to `/sign-in?next=<path>` and idle-timeout (30 min default) to `/sign-in?expired=1&next=...` |
| Overview | `/` | ✅ | KPIs · throughput + status charts · FX spread health · services grid · 20-row recent activity |
| Transfers Monitor | `/operations/transfers` | ✅ | 200-row mock · filters · sort · pagination · bulk actions · saved filters · Export CSV |
| Transfer Detail | `/operations/transfers/:id` | ✅ | 8 left cards · sticky right rail · 6 action modals · real-time refresh sim · 11 hotkeys |
| KYC Review Queue | `/operations/kyc-queue` (+ `/:id`) | ✅ | Master-detail · 30-row mock · 4 edge classes · 4 action modals · 8 hotkeys |
| AML Triage | `/operations/aml-triage` (+ `/:id` + `/new`) | ✅ | Master-detail · 26-flag mock · 5 typed contexts · 4 action modals · manual-flag form · 6 hotkeys |
| Customers — Users | `/customers/users` (+ `/:id`) | ✅ | List + full-page detail · 50-row mock · 8 tabs (`?tab=...` deep-link) · 7 admin actions · 4 contextual modals · cross-store sync to AML on block/soft-delete · MyID profile card (5 sections + raw JSON) · lifetime stats derived from `TRANSFERS_FULL` so KPIs / charts / recent activity / Transfers tab stay consistent · MyID enforced as the hard gate for transfers (`tier_0` / `tier_1` are partial-registration with 0 limits) |
| Customers — Cards | `/customers/cards` (+ `/:id`) | ✅ | List + full-page detail · 80-card mock across 33 owners (73 active / 4 frozen / 2 expired / 1 removed) · 6-filter chip row (scheme · status · bank · country · last-used date-range picker · default-only / never-used toggles) · sortable desktop table + mobile card stack · CSV export · 4-button action bar (Freeze ↔ Unfreeze · Copy token · Open transfers) · 2 action modals (Freeze · Unfreeze) — Force-expire dropped (admin-unilateral termination conflicts with the user-only-unlinks policy) · `?context=card&card_id=` filter on Transfers list + transfer-detail pager · cross-store sync via `mockCards.freezeAllUserActiveCards` invoked from `mockUsers.blockUser` |
| Customers — Recipients | `/customers/recipients` (+ `/:id`) | ✅ | List + full-page detail · 60-recipient mock across 30 distinct owners (36 alipay · 24 wechat · 12 favorites) · `mockRecipients.ts` is single source of truth · 3-filter chip row (destination · favorites-only · last-used `<DateRangePicker>` per Cards convention) · sortable desktop table (created · last-used · transfer-count) + mobile card stack · CSV export · full-bleed detail page (display info · owner · usage card with 4 KPI tiles + last-5 transfers + "View all" CTA) · single Hard-delete action with 2-step Dialog → AlertDialog confirm and ≥20 char reason · `?context=recipient&recipient_id=` filter on Transfers list + transfer-detail pager + back-link · `getUserRecipients` / `hardDeleteRecipient` in `mockUsers` are now thin wrappers · CJK display names render verbatim · 4 page-scoped hotkeys (j/k/Enter/`/` on list; b/Backspace/Del on detail) |
| Compliance — Blacklist | `/compliance/blacklist` (+ `/new` + `/:id`) | ✅ | List + full-page Add form + full-page detail · 5 type tabs (Phone / PINFL / Device / IP / Card token) with count badges · 45-row deterministic mock (8 phone · 4 pinfl · 12 device · 15 ip · 6 card-token) seeded with realistic fraud/compliance reasons · `mockBlacklist.ts` is the single source of truth · canonical filter bar (status · added-by · created date-range · search) · sortable desktop table (created · expires) + mobile card stack · pre-add check panel (matches against current `mockUsers` / `mockCards` for phone / pinfl / card_token; calm "no live store" copy for device / ip per prototype scope) · duplicate detection blocks submit · per-type identifier validation (E.164 / 14-digit / hex / IPv4-or-IPv6 / token ≥ 8) · severity radio (suspected / confirmed) · optional expiry via shared `<DateTimeInput>` primitive · ≥30-char reason note · AlertDialog "Add this entry now?" confirm · 3-action detail bar (Edit reason · Extend expiry · Remove) — Edit + Extend are small Dialogs, Remove is AlertDialog with ≥30-char reason · impact card derives "currently affecting" from cross-store lookups for phone/pinfl/card_token + seeded `affectedCount` for device/ip + optional `loginAttemptsBlocked30d` line · audit-log mutators wired (`add` / `edit_reason` / `extend_expiry` / `hard_delete`) and bridged into `mockAuditLog` so blacklist actions appear on the central Audit Log surface · back-compat redirects from `/blacklist` and `/blacklist/new` (preserving `?type=&identifier=` query params for the existing Users-detail "Add phone to blacklist" deep-link) · `g+b` global hotkey routes here · 5 page-scoped hotkeys (n / j / k / Enter / `/` on list; b / Backspace / Del on detail; Cmd/Ctrl+Enter on Add) |
| Compliance — KYC Tiers reference | `/compliance/kyc-tiers` | ✅ | Read-only reference (no edit affordance, no version history). 3 tier cards (badge / name / `tier_*` code / description) — tier_2 carries the full stats grid (per-tx · daily · monthly · max-cards) + green "MyID required" chip; tier_0 + tier_1 carry only a warning-toned gate note (tier_0: "cannot access the app, phone OTP first" · tier_1: "view-only — services and FX rates visible, no operations until MyID") since no operations exist at those tiers in live system behavior. `LiveImpactCard` derived from `mockUsers` + `mockTransfers` (total users on top, 3-cell tier-count grid, active-transfers + avg per-tx amount strip). `mockKycTiers.ts` is a constants-only module (no mutator, no audit, no version history) — limit values are placeholder per `kyc-tiers-and-limits.md` pending Compliance sign-off. Back-compat redirect from `/kyc-tiers`; sidebar repointed |
| Compliance — Audit log | `/compliance/audit-log` | ✅ | Read-only union view · 200-row deterministic 7-day seed (~80 system / ~50 provider / ~40 admin / ~30 user) plus live bridge from 6 module audit stores (mockUsers / mockCards / mockKycQueue / mockAmlTriage / mockFxRates / mockCommissionRules) AND `TRANSFER_EVENTS_FULL` so admin actions taken in the session show up here · `mockAuditLog.ts` is the bridge + seed · non-sticky filter bar (canonical full-width entity-ref search row matching Users / Cards / Recipients pattern w/ inline X clear · chip row beneath: DateRangePicker default Today · 4 actor types · admin actor searchable select · 11 entity types · 12 actions) · sortable desktop table with chevron + click-to-expand inline rows + 9 columns (timestamp / actor type / actor / action / entity type / entity ref / from→to transition pill / 80-char context summary) · mobile card stack mirror with same data density · expanded body (full timestamp / actor block (id / name / phone / ip / device) / entity link / reason note / collapsible JSON viewer with Copy / "View N other events for this entity" CTA that scopes the page to that entity) · in-place copy feedback (Copy→Check icon swap + success-tinted color flip for 1.5s via shared `useCopyFeedback` hook — no toast) on every entity-ref / context copy button · DateRangePicker primitive listens to `matchMedia('(max-width: 767px)')` so it renders single-month on mobile + 2-month on `md+` (applies to every consumer of the primitive) · CSV / NDJSON export modal with include-context toggle and ≥2 MB size warning (export modal still uses toasts since they communicate background work) · 5 page-scoped hotkeys (j/k/Enter/e/f) · `?entity=&id=` deep-link auto-applies and is stripped after consumption · `g+l` global hotkey routes here |
| Finance — FX config | `/finance/fx-config` (+ `/new`) | ✅ | List + full-page Update form · 19-version mock spanning 60 days (incl. one historic 1.5% manual-override version) · `mockFxRates.ts` is single source of truth (immutable rows — `addFxRate` only inserts new versions and closes the previous active window) · active rate card with 4-cell grid (mid · spread · client · source) + health badge (Healthy / Drifting / Stale based on spread bands + `validTo` expiry) · trend chart with 24h/7d/30d/90d range tabs (mid line dashed slate, client line solid brand) · sortable version history table with inline expand showing full record + diff vs previous · mobile card stack mirrors the table · Update form with Source select (central_bank locks numeric inputs · provider_x / manual unlock) · `<StepperNumberInput>` (↑↓ ±0.01, Shift+↑↓ ±0.10) · auto-computed client rate · 2-pane layout (form left, sticky DiffPreview right · mobile "Show diff" toggle) · ≥20-char reason note · AlertDialog confirms with in-flight count line · `setInFlightCounter` wiring keeps mockFxRates free of a circular import on mockTransfers · Cmd/Ctrl+Enter submit · `u` hotkey from list opens Update page |
| Finance — Commission rules | `/finance/commissions` (+ `/new`) | ✅ | List + full-page New-version form · Tabs split Personal vs Corporate · 12 personal versions + 8 corporate versions deterministic mock · `mockCommissionRules.ts` is single source of truth (immutable rows — `addCommissionRule` only inserts new versions per accountType and closes the previous active window) · Active rule card with 4-cell grid (personal) / 6-cell (corporate adds volume_threshold + corporate_pct) · live worked example on a 5,000,000 UZS sample (commission %, commission UZS, min-fee floor check, total fee + corporate above-threshold variant) · sortable version history table with inline expand showing full record + diff vs previous + reason note · mobile card stack mirrors the table · New-version form pre-fills from active rule · Lifted `<StepperNumberInput>` (↑↓ ±0.01 / ±100, Shift+↑↓ ±0.10 / ±1000) and `<DateTimeInput>` to `components/zhipay/` so commissions can consume without a Pattern→Pattern import · 4-rule client-side validation (min ≤ max, min_fee ≥ 0, from < to, corporate_pct ≤ min_pct) · ≥20-char reason note · 2-pane layout (form left, sticky DiffPreview + WorkedExample right · mobile "Show diff & worked example" toggle) · AlertDialog confirm with effective_from line · Cmd/Ctrl+Enter submit · `n` hotkey from list opens New-version page for the active tab · `g+m` global hotkey routes here |
| System — Services & Health | `/system/services` (+ `/:id`) | ✅ | Two-pane on `lg+` (grid left + detail pane right) · grid-only on `<lg` w/ tile click → `/:id` full page · 5 services (alipay · wechat · uzcard · humo · myid; Visa/MC absent per v1 scope) · `mockServices.ts` is single source of truth — `ServiceFull` shape extends schema §8 with mock-only observability cache (latency P50/P95/P99 24h · success 24h+7d · uptime 30d · inflight count · webhooks/hr · 24-pt success sparkline · 20-pip health-check strip · 10 webhook events · 5 latency-spike alerts) · ServiceTile w/ logo + status badge + health-overlay dot when configured-status disagrees with observed health · selected tile = `ring-2 ring-brand-600 bg-brand-50/60` · DetailHeader w/ 3-segment status toggle (clicking a non-active segment opens AlertDialog — no immediate state change) · QuickStatsCard · HealthChecksCard w/ 20-pip strip (hover tooltip = timestamp + ms) + URL copy (in-place `Copy → Check` feedback via shared `useCopyFeedback` hook lifted to `hooks/`) + Run-now CTA · ConfigCard collapsible jsonb viewer w/ sensitive keys masked (`api_key` / `secret` / `signing` / `private_key` / `cert_serial_no` / `api_v3_key` / `jwt_signing_key`) and **no reveal affordance** — pure server-side data · Edit-config link visible but disabled (out of v1 scope) · RecentActivityCard w/ webhooks list + latency-spike alerts · canonical action-bar variants (sticky inside right pane on `lg+` · `fixed inset-x-0 bottom-0 md:left-[var(--sidebar-width,4rem)]` overlay on mobile per LESSON 2026-05-02) · 3-mode StatusChangeDialog: → Maintenance ≥30 char reason; → Disabled ≥50 char reason + "I understand the impact" checkbox + destructive CTA (body shows live inflight count); → Active ≥20 char reason · `setServiceStatus` mutator emits one audit-log row w/ `{from_status, to_status, inflight_at_change, acknowledge_impact?}` and bridges via `bridgeServiceAudit` → central `mockAuditLog` (granular `activate` / `enter_maintenance` / `disable` / `run_health_check` verbs preserved in `context.kind`; spec's 12-value enum maps `activate / enter_maintenance / disable → status_changed`, `run_health_check → updated`) · 30s tile-metric tick refreshes `formatRelative` timestamps; detail-pane stat values frozen at-last-action time per spec · 350ms initial-mount skeletons · Visa / Mastercard absent per LESSON 2026-04-30 · 5 page-scoped hotkeys on detail (`1` Active / `2` Maintenance / `3` Disabled — each opens confirm; `r` Run check; `Esc`/`Backspace`/`b` close detail) · `g+s` global hotkey re-pointed to `/system/services` · back-compat redirects from `/services` and `/services/:id` |
| System — App versions | `/system/app-versions` | ✅ | Two-tab list (iOS / Android) under `?platform=` deep-link · 9 iOS + 9 Android deterministic mock (1.4.2 latest down to 1.2.0 force-update security release; uz/ru/en notes are natural translations, not transliterations) · `mockAppVersions.ts` is single source of truth (immutable rows — `addAppVersion` rejects duplicates, `editAppVersion` preserves prior values in `previous_*` on the audit row) · ActiveVersionBanner per platform (filled brand-tinted card · platform glyph · version + force-update pill + `min_supported` + Edit) · sortable desktop table (released_at) with click-to-expand inline RowExpanded showing all three locales markdown-rendered + last-edited line · mobile card stack mirror · ReleaseNotesPreview cell = 3 Wikimedia raster flag chips (UZ / RU / GB-1707) + 60-char en truncation · responsive AddVersionDialog (full-screen `<sm`, 640 × 85vh `sm+`) with Edit / Preview tabs in the release-notes section · ReleaseNotesEditor uses a locale tab strip with Cmd/Ctrl+1/2/3 hotkeys, autoresizing textarea, char counter, per-locale empty-required validation · ReleaseNotesPreviewPane renders one faux App Store / Play Store update card per locale via tiny zero-dep markdown transformer (`* / -` → `<ul>`, blank line → `<p>`) · DateTimeInput drives `releasedAt` (default `now()`) · platform radio + semver input + min-supported input + force-update toggle with inline danger-tinted warning banner that interpolates the entered min_supported · 4-rule client-side validation (semver · `(platform, version)` uniqueness blocks submit · `min_supported ≤ version` · all three locales non-empty) · Cmd/Ctrl+Enter submits when valid → AlertDialog 2-step confirm "Publish {platform} v{version} now?" · EditVersionDialog shares the same shape with platform + version locked, + ≥20-char reason note card (matches Users / Cards / Services convention) · audit-log mutators wired (`add` / `edit`) and bridged via `bridgeAppVersionAudit` → central `mockAuditLog` (mapped to `created` / `updated`; granular verb in `context.kind`; snapshot + per-field `previous` preserved) · row kebab "Open audit log entry" deep-links to `/compliance/audit-log?entity=app_version&id=...` · 3 PlatformIcon / Edit pills follow consistent fill: PlatformIcon swapped to provided 50×50 SVG silhouettes (full Android robot + Apple bitten silhouette, `currentColor` fill so tone wiring still drives color) · LocaleFlag uses Wikimedia rasters (uz / ru / gb-1707) bundled via Vite asset pipeline (`uz.png` hashed, `ru.png` + `gb.png` inlined as base64 under 4KB threshold; `loading="lazy" decoding="async"`) · 350ms initial-mount skeletons match FX / Services cadence · per-platform sort + expanded-row cached via module-level `filterState` so tab switching preserves UI state · `n` page-scoped hotkey opens Add modal · `g+v` global hotkey routes here (since `g+a` is taken by AML Triage) · sidebar / TopBar breadcrumbs / CommandPalette repointed · `RedirectPreservingQuery` back-compat from `/app-versions` |
| System — Error codes | `/system/error-codes` | ✅ | Read-only catalog at `/system/error-codes` · 15-row deterministic seed in `mockErrorCodes.ts` (canonical codes spanning all 6 categories — uz/ru/en authored as natural locale-appropriate translations, not transliterations) · per-code synthetic observability cache (lastTriggeredAt + 30-day daily counts) modelling what a metrics store would surface — derived deterministically per code-string seed, **not** from `mockTransfers` · ReadOnlyBanner with deployment-only edit copy · canonical filter bar (full-width search across code / message_* / suggested_action_* in all 3 locales · 6-category multi-chip · retryable single-chip with any/yes/no) · sortable desktop table (`code` ASC default; non-sticky thead per LESSON 2026-04-30; chevron / Code mono / Category chip 6-tone / Retryable chip / 80-char `message_en` preview) + mobile card stack mirror · CategoryChip palette per spec D9: kyc=brand · acquiring/fx/provider=warning · compliance=danger · system=slate · click-to-expand inline RowExpanded showing 3 locale message cards + 3 locale suggested-action cards (each with shared `<LocaleFlag>` + uz/ru/en label) + WhereReferencedLinks + LastTriggeredCard · WhereReferencedLinks: always renders `/operations/transfers?failure_code=...` deep-link; renders `/compliance/aml-triage?type=sanctions` deep-link only on `SANCTIONS_HIT` (no clean correlation for other codes) · LastTriggeredCard: relative-time chip + 3 stat tiles (24h/7d/30d) + 7-day daily-count sparkline (recharts mini LineChart, no axes, brand-600 stroke, 13px floor honored) · Export CSV (9 cols: code/category/retryable + 3 locale message + 3 locale suggested action) · `g+e` global hotkey routes here · 4 page-scoped hotkeys (`/` focus search · `f` focus first chip · `j/k` row focus · `Enter` expand) · 350ms initial-mount skeletons (matches KycTiers cadence) · `LocaleFlag` lifted from `app-versions/` to `components/zhipay/` (Pattern→Pattern→shared) — 4 app-versions consumers repointed (`ReleaseNotesPreview`, `ReleaseNotesEditor`, `ReleaseNotesPreviewPane`, `RowExpanded`) · `failure_code` filter dimension added to `TransferFilters` + Transfers list reads `?failure_code=` URL param on mount, seeds the filter, strips the param, and renders a dismissable brand-tinted banner above the existing card/recipient context banners · back-compat redirect from `/error-codes` |
| Content — Stories | `/content/stories` (+ `/new` + `/:id`) | ✅ | Card-grid CMS at `/content/stories` (first `/content/*` route in the codebase) · 12-record deterministic seed in `mockStories.ts` (4 published / 4 scheduled / 4 drafts; themes: Add card 30s, Lower fees, WeChat 30s, MyID, Navruz, Rate-lock, Save recipients, Refer a friend, Trader bulk-send, Welcome, Default card, Ramazan rates; uz/ru/en authored as natural locale-appropriate translations) · status is **derived**, not stored — 4-value enum (`draft` / `scheduled` / `published` / `expired`) computed from `is_published × published_at × expires_at` · responsive grid (`grid-cols-1` mobile / `md:grid-cols-2` / `lg:grid-cols-4`) · StoryCard w/ 16:9 MediaPreview (gradient-from-URL placeholder + Play overlay for video — real backend would stream first frame) + StatusChip top-left + drag-handle top-right (published only) + title + chips row (TypeChip / display-order / cta-deep-link preview) + expires/scheduled meta (red when <24h) + footer kebab (Edit / Publish-now / Delete) + Edit→ link · drag-to-reorder via `@dnd-kit/core + sortable + utilities` (added to package.json — keyboard-accessible space-to-grab + arrows native) restricted to published group · ReorderConfirmDialog on drop shows old→new slot visual + ≥20-char reason note before commit · `reorderStory()` shifts conflicting published rows by one to satisfy the partial unique index · DeleteStoryDialog 2-step (Dialog reason ≥20 chars + danger-tinted warning banner → AlertDialog "Are you sure?" before hard-delete) · PublishNowDialog single-step AlertDialog for draft/scheduled override · canonical filter bar (status multi · type multi · has-expiration toggle chip) + Sort dropdown (Display order / Created / Expiring soon — display-order grouping = published by displayOrder ASC → scheduled by displayOrder ASC → drafts by createdAt DESC → expired by expiresAt DESC) · empty / filtered-empty / 8-skeleton-loading states · 4 page-scoped hotkeys (`n` new · `j`/`k` card focus · `Enter` open editor) · `g+y` global chord routes here · **Editor** at `/content/stories/new` (+ `/content/stories/:id` for edit): two-column layout (form left 60% / sticky StoryPhonePreview right 40% on `lg+`; mobile = floating Eye button opens MobilePreviewSheet bottom sheet w/ same preview) · form sections: MediaTypeSection (image/video radio cards + URL input w/ http(s) validation) → TitlesSection (LocaleTabInputs: locale tab strip + single-line `<Input>`, all 3 locales required, 80 char max) → DisplayOrderSection (StepperNumberInput integer ±1/±5 + "Next available slot: #N" hint) → CtaSection (Switch toggle + LocaleTabInputs for labels 40 char max + DeepLinkBuilder w/ Select primitive for 8-screen taxonomy + ParamsEditor mono textarea w/ JSON.parse blur validation + per-screen params hint + previewDeepLink string `zhipay://...` + Copy w/ in-place feedback) → ScheduleSection (paired `<DateTimeInput>` for publish_at + expires_at) → ReasonSection (edit-only ≥20-char note) · StoryPhonePreview = 9:16 PhoneMockup primitive (notch + status bar w/ illustrative SVG glyphs only, no sub-13px text per LESSON 2026-04-29) + title overlay top + CTA pill bottom + 3-segment progress strip · locale switcher above preview drives `previewLocale`, also auto-syncs from form's active editing locale · Cmd/Ctrl+1/2/3 cycles locale tabs (also flips preview), Cmd/Ctrl+S save draft, Cmd/Ctrl+Enter publish · 4-rule validation (titles all 3 locales · cta labels all 3 when enabled · expires_at > publish_at · displayOrder integer non-negative) · sticky-bottom EditorFooter w/ canonical `fixed inset-x-0 bottom-0 md:left-[var(--sidebar-width,4rem)]` pattern · PublishConfirmDialog adapts copy: "Publish now?" for immediate / "Schedule for {when}?" when publish_at in future / "Apply update?" for edit · mutators: `addStory` / `editStory` (≥20 char reason, captures per-field `previous`) / `publishStory` (sets is_published + publishedAt + auto-fills displayOrder if 0) / `unpublishStory` / `reorderStory` (partial-unique-aware shift) / `deleteStory` (hard-delete w/ snapshot) — all bridged via `bridgeStoryAudit` → central `mockAuditLog` (5 actions: add→created · edit/reorder→updated · publish/unpublish→status_changed · delete→deleted; granular verb in `context.kind`; snapshot + per-field `previous` preserved; `from_status`/`to_status` set for publish/unpublish) · `AuditEntityType` += `'story'` · `entity-type` chip + label key added to AuditLog filter · row "Open entity" link wired to `/content/stories/:id` · `LocaleTabTextarea` lifted from `components/app-versions/modals/ReleaseNotesEditor` to `components/zhipay/LocaleTabTextarea` (3rd-consumer rule fired — Stories is consumer #3 alongside App Versions) · App Versions Add/Edit dialogs repointed to use lifted component w/ surface-scoped i18n key props · new shared `lib/deepLinkScreens.ts` constants module (8-value taxonomy + label/hint i18n keys + `previewDeepLink()` helper) · new `Switch` + `Select` ui primitives based on existing `@radix-ui/react-switch` + `@radix-ui/react-select` deps · sidebar entry repointed (`/stories` → `/content/stories`); TopBar `ROUTE_TITLES` repointed; CommandPalette gained Stories entry under Navigate w/ `g y` shortcut hint · back-compat redirects from `/stories`, `/stories/new`, `/stories/:id` |
| Content — News | `/content/news` (+ `/new` + `/:id`) | ✅ | Article-list CMS at `/content/news` (second `/content/*` route after Stories) · 27-record deterministic seed in `mockNews.ts` (23 published + 4 drafts; uz/ru/en bodies hand-authored as natural locale-appropriate translations in press-release tone) · status is **derived** from `is_published` (2 values: `published` / `draft`) · sortable list w/ default sort = drafts first by `createdAt DESC`, then published by `publishedAt DESC` · canonical filter bar (debounced 300ms title search across uz/ru/en + status multi-chip + `<DateRangePicker>` for `publishedAt`) + Sort dropdown (Default / Published date / Created date) · desktop table on `lg+` (60×40 image preview / Title with `LocaleFillIndicator` flag chips for uz/ru/en fill state / StatusChip / Published at column with formatted timestamp + relative / Created at / kebab) + mobile card stack on `<lg` (16:9 left rail image + title + status + locale-fill chips) · 8-row skeleton loading state · empty / filtered-empty states · 4 page-scoped hotkeys (`/` focus search · `f` focus first chip · `j/k` row focus · `Enter` open editor · `n` new article) · `g+n` global chord routes here (rebound from former `/notifications` placeholder; Notifications will get a different chord when its phase lands) · **Editor** at `/content/news/new` (+ `/content/news/:id` for edit): two-column layout (form left 60% / sticky `NewsPhonePreview` right 40% on `lg+`; mobile = floating Eye button opens `MobilePreviewSheet` bottom sheet w/ same preview) · form sections: CoverImageSection (URL input w/ `https?://` validation + 16:9 inline preview when valid, optional) → TitlesSection (lifted `LocaleTabInputs`: locale tab strip + single-line `<Input>`, all 3 locales required to publish, 120 char max with warning at >100) → BodiesSection (locale tab strip with red-dot indicators + `RichTextEditor` w/ TipTap toolbar — Bold/Italic/Underline + H2/H3 + bullet/numbered lists + Link dialog (URL validation) + Quote + Horizontal divider + Inline image dialog (URL + alt text); 200-char plain-text minimum to publish) → PublishSection (Switch toggle + paired `<DateTimeInput>` for `publishedAt`; locked when article was already published — changes happen via Update) → ReasonSection (edit-only ≥20-char note shown when editing an already-published article) · NewsPhonePreview = 9:16 lifted `PhoneMockup` primitive + faux app top bar (back chevron + "News" eyebrow + share) + cover image strip + title + meta + body rendered via `dangerouslySetInnerHTML` with TipTap-compatible prose classes (matches `NEWS_EDITOR_PROSE_CLASSES`) · locale switcher above preview drives `previewLocale`, auto-syncs from form's active editing locale · Cmd/Ctrl+1/2/3 cycles locale tabs (also flips preview), Cmd/Ctrl+S save draft, Cmd/Ctrl+Enter publish · validation: drafts may be partial (≥1 locale filled with title + body); publish requires all-3-locales valid + image URL ok + reason ≥20 if edit-on-published · sticky-bottom `EditorFooter` w/ canonical `fixed inset-x-0 bottom-0 md:left-[var(--sidebar-width,4rem)]` pattern + back-link "Back to news" w/ canonical `<ArrowLeft>` icon (no `← ` literal per LESSONS 2026-05-02) · `PublishConfirmDialog` adapts copy: "Publish article now?" / "Apply update?" (interpolates `formatDateTime` for the publish stamp) · mutators: `addNews` / `editNews` (≥20 char reason when target is published, captures per-field `previous`) / `publishNews` / `unpublishNews` / `deleteNews` (≥20 char reason w/ 2-step Dialog → AlertDialog confirm) — all bridged via `bridgeNewsAudit` → central `mockAuditLog` (5 actions: add→created · edit→updated · publish/unpublish→status_changed · delete→deleted; granular verb in `context.kind`; snapshot of `{titleEn, isPublished, publishedAt, hasImage}` + per-field `previous` preserved; `from_status`/`to_status` set for publish/unpublish) · `AuditEntityType` += `'news'` · entity-type chip + label key added to AuditLog filter (now 13 entity types) · `ENTITY_LINK['news']` wired to `/content/news/:id` · **deps added**: TipTap suite (`@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-link` + `@tiptap/extension-image` + `@tiptap/extension-underline` + `@tiptap/extension-placeholder`) — body stored as HTML strings (sanitized server-side per real backend) · **lifts**: `PhoneMockup` lifted from `components/stories/` → `components/zhipay/` (News editor preview = 2nd consumer); `LocaleTabInputs` lifted from `components/stories/editor/` → `components/zhipay/` (News titles = 2nd consumer; Stories' FormSections + StoryPhonePreview repointed to shared) · ~85 new `admin.news.*` i18n keys (page chrome / filter / sort / status / table columns / row kebab / 3 dialog bundles / editor sections + fields + toolbar + link/image dialogs + publish + reason + validation + preview + footer + confirms + not-found) · sidebar entry repointed (`/news` → `/content/news`); TopBar `ROUTE_TITLES` repointed; CommandPalette gained News entry under Navigate w/ `g n` shortcut hint · back-compat redirects from `/news`, `/news/new`, `/news/:id` · removed `/news` from `PLACEHOLDER_ROUTES` · schema cascade in `docs/models.md` §8 (NEWS body fields clarified as rich-text HTML; `image_url` marked nullable; `published_at` clarified as nullable until publish) + §9.2 (added `(is_published, published_at DESC NULLS LAST)` listing index) |
| Settings — Profile · Security · Sessions · Preferences · My audit | `/settings` | ✅ | Single page with five tabbed sections (`?tab=profile|security|sessions|preferences|my-audit`, default `profile`) reachable from the TopBar avatar dropdown. Full-bleed (no max-width per LESSON 2026-04-29). Page header (inline, non-sticky per LESSON 2026-05-02) + inline tab strip (NOT sticky — title, tabs, and body scroll together as one unit; mobile horizontal-scroll on overflow). **Profile** tab: 72×72 avatar (initials, "Change avatar" disabled with tooltip — initials-only in v1) + editable display name + read-only email with "Managed out-of-band" chip + editable phone + read-only role chip + read-only created-at + Save button (disabled until change) → `<NameChangeReasonDialog>` AlertDialog requires ≥10-char reason. **Security** tab: Password card (Last changed Nd ago — danger-tinted text when >90d) + `<ChangePasswordModal>` (current + new w/ `<PasswordStrengthMeter>` against 4 rules: ≥12 chars · mixed case · number · symbol; confirm match; AlertDialog "You will be signed out of all other sessions" before commit; Cmd/Ctrl+Enter submit). **No 2FA card** — admin auth is email + password only per Phase 20 direction "the otp is for mobile auth"; the schema documents the absence and the surface honors it. Recovery card = read-only "out-of-band only" copy. **Sessions** tab: `<ActiveSessionsList>` desktop dense table / mobile stacked cards (Device w/ laptop·phone·tablet glyph + browser + OS · IP + ~city · Signed in relative+absolute · Last activity relative · "This device" chip on current row · Revoke button on every other row, disabled on current) + "Revoke all other sessions" warning button → AlertDialog. `<SignInHistoryCollapsible>` (default closed) renders the last 30 sign-ins (success + failed) inline as a non-sticky-thead table per LESSON 2026-04-30. **Preferences** tab: Display card (Theme radio Light/Dark/System — applies live via existing `<ThemeProvider>` · Language radio English/Russian (Coming soon)/Uzbek (Coming soon) · Density radio Compact/Comfortable — applies live via `data-density` attribute on `<html>` threaded into `<TableRow>` via `--row-h` CSS var so every existing table reflects the change · Tabular numerals toggle — applies live via `data-tabular-nums`), Time & locale card (Timezone Select default browser-detected · Date format radio · Time format radio — persisted but date-format full rollout deferred to next phase, inline note explains), Notifications card (6 toggle rows: AML critical · Sanctions hit · Service offline · FX stale · Daily digest · Failed sign-in to my account) + Save button at bottom. **My audit** tab: quick-stat row ({N} actions in last 24h · {M} in last 7 days) + filtered view of central audit log scoped to current admin's actions (entity-state-change events only — auth events live in `admin_login_audit`). **Sample data**: `mockAdminAuth.ts` super-admin renamed Yulduz Otaboeva → Sardor Tursunov (`sardor@zhipay.uz` · phone `+998 90 123 45 67` · created 8mo ago · last password change 47d ago); `mockAdminSessions` seeds 2 active sessions (current Chrome/macOS/Tashkent + iOS app signed-in 2 days ago); `mockAdminSignInHistory` seeds 30 entries spanning the last 14 days. **Audit cascade**: 4 new `admin_login_event` enum values (`profile_changed` / `password_changed` / `session_revoked` / `session_revoked_all`); preferences changes are NOT audited (cosmetic). **Critical rules**: email never editable (out-of-band only) · role never editable (out-of-band only) · current session never self-revocable from this surface (use TopBar Sign out) · regenerating backup codes / 2FA reset are deferred (see "no 2FA" note above). **Density var rollout**: `--row-h` set on `[data-density=compact]` (40px) and `[data-density=comfortable]` (44px) in `globals.css`; `<TableRow>` reads `style={{ height: 'var(--row-h, 40px)' }}` so every existing table responds. `--tabular-nums` toggles `font-variant-numeric: tabular-nums` on `body`. **Routing**: new `/settings` route inside `<AuthGuard>`-wrapped `<AppRoutes>`; UserMenu's "Profile" item replaced with "Settings" → `/settings`. **Keyboard**: new `g+,` global chord routes here (Mac Cmd+, convention); `1`–`5` page-scoped chords jump tabs; `t` (global) toggles theme; Cmd/Ctrl+Enter inside Change-password modal submits. Latent `g+i → /content/notifications` chord binding fixed (was documented in HelpOverlay but not bound in handler). **Schema cascade**: `models.md §10.2` extended (`phone` / `last_password_changed_at` / `recovery_contact` / `preferences` jsonb fields documented); new `§10.8` documents the `preferences` jsonb shape; new `§10.9` documents the 4 new audit event types. New mermaid `admin_session_revocation_flow.md`. PRD §6.1 row 15 + Gherkin AC fragment. ~60 new `admin.settings.*` i18n keys (EN-only matching project i18n stub) |
| System — Error & system states | `/*` (catch-all 404) · `/system/403` · `/system/preview/*` · `/system/shortcuts-print` | ✅ | `<NotFound>` (404) replaces the previous `<Placeholder>` catch-all — in-shell variant w/ "Back to Overview" + "Open command palette (⌘K)" secondary CTA wired via `<AppShellContext>`; full-bleed variant for deep-link-before-sign-in; footer renders requested path mono. `<ServerErrorState>` (500/503) rendered by `<SystemErrorBoundary>` on render-time crashes — generated 8-char reference id (`8a7c-2f1e` format) shown in the footer w/ in-place copy feedback (`useCopyFeedback`-style — Copy→Check icon flip, no toast); Try-again resets boundary + falls back to `window.location.reload()` in preview mode. `<ForbiddenState>` (403, defensive RBAC fallback at `/system/403`) — no reference id (permission outcome, not a system error); logs `permission_denied` to `mockSystemEvents` on mount (skipped in preview). `<MaintenanceState>` (full-page) — replaces entire shell when `useMaintenanceState().active` is true; reads `?maintenance=on` URL param at boot via `bootMaintenanceFromUrl()` + persists in localStorage; mini timeline shows "Started at" + "Estimated end" via `formatDistanceToNow`; refresh-status button bumps notify so relative-time strings re-render. `<OfflineBanner>` — slate-toned banner inside AppShell, renders when `navigator.onLine` is `false`; stale data stays visible below; preview supports `forceVisible` + `cachedFromOverride`. Session-lost path: `triggerSessionLost(currentPath)` exposed from `lib/auth.ts` for future fetch interceptors → calls `signOut({reason:'session_expired'})` + redirects to `/sign-in?next=...&reason=session-lost`; SignIn reads `?reason=session-lost` (in addition to existing `?expired=1`) and shows the existing `SessionExpiredBanner`. **Shortcuts overlay** (`?` chord) — HelpOverlay i18n keys migrated from `admin.help.*` to `admin.shortcuts.*` namespace; subtitle updated to "Press ? anywhere to open this. Press Esc to close."; SHORTCUTS catalogue lifted to `components/layout/shortcuts.ts` so the overlay and the new `/system/shortcuts-print` route share one source. **Print shortcuts** = clean 2-column print layout that auto-fires `window.print()` 250ms after first paint; opened in a new tab from the HelpOverlay footer link. **Preview routes** at `/system/preview/*` (7 children: 404 / 500 / 403 / offline / maintenance / session-lost / shortcuts) — dev-tooling surface for eyeballing each state with deterministic mock data ("requested: /operations/transfersss" · "req-id: 8a7c-2f1e" · "started 8m ago, est end in 22m"); not exposed in sidebar / command palette. **Audit decision (locked)**: `mockSystemEvents` is a separate forensic store, NOT bridged into central `mockAuditLog` — keeps the central log focused on entity-state-change events for compliance reviewers. **i18n**: ~37 new `admin.error.*` / `admin.system.*` / `admin.shortcuts.*` keys (EN-only matching project i18n stub); 2 retired `admin.help.*` keys removed. **Routing**: catch-all `<Route path="*">` inside `AppRoutes` now renders `<NotFound />`; `<SystemErrorBoundary>` wraps `<Routes>`; `<MaintenanceGate>` wraps the top-level `<Routes>`. **Offline write gating** (Phase 22b): `<WriteButton>` primitive in `components/zhipay/` — drop-in replacement for `<Button>` that disables itself + surfaces a tooltip ("Reconnect to perform this action") when `useNetworkState()` is offline. Applied across 7 sticky-bottom action bars (KYC Queue · AML Triage · Cards Detail · Recipients Detail · Blacklist Detail · Notifications Compose · Notifications Cancel-Scheduled) — primary positive-action and destructive buttons gated. Domain-disabled buttons (KYC Approve / AML Clear, gated by tier rules / sanctions) keep their existing reason-tooltip wrap; offline gating layers in only when the button isn't already domain-disabled. **Offline action queue** (Phase 22b): `lib/offlineActionQueue.ts` exposes `tryWriteOrQueue({label, executor})` — when online, executes synchronously; when offline, queues for replay. On `window.online` event, drains the queue + fires a single `Synced N actions` toast. Wired as a demo to AML Triage's Assign-me chord (the `m` keyboard chord bypasses the WriteButton UI gate, so the queue catches it). New `admin.system.offline.toast.queued` i18n key surfaces an info toast on queue. **Maintenance draft preservation** (Phase 22b): `useDraftPreservation({key, value, setValue, serialize, deserialize})` hook — auto-saves form state to localStorage (debounced 500ms), restores on mount, surfaces a toast ("We saved your draft locally — it'll be available when maintenance ends.") when `useMaintenanceState().active` flips false→true. Applied as a demo to the Notifications Compose form with custom (de)serializer that re-hydrates the `scheduledFor: Date` field per LESSON 2026-05-03. Successful submit calls `clearDraft()` to wipe the saved draft. **Outside-shell 404 organic flow** (Phase 22b): new `PathAwareAuthGuard` wraps the catch-all `*` route. When the request is signed-out AND the path doesn't match any known top-level prefix (`/operations/*`, `/customers/*`, `/finance/*`, `/compliance/*`, `/system/*`, `/content/*`, `/settings`, plus back-compat flat routes), it renders the full-bleed 404 directly — without first redirecting to `/sign-in?next=...`. **No PRD / models / mermaid cascade** — the operational shape is mock-only and doesn't add new entity types or state machines |
| Content — Notifications | `/content/notifications` (+ `/new` + `/sent/:id`) | ✅ | Admin push-notification composer (dedicated `/new` route per Phase 19a restructure — original Phase 19 shipped a tabbed Compose / Sent shell; restructured to match Stories / News editor sibling pattern) + sent-history list + per-row sent-detail · 38-row deterministic seed in `mockNotifications.ts` (32 sent + 4 scheduled + 2 cancelled) covering 4 types × 3 audience types in realistic mix (~50% transfer-single transactional · ~25% promo-broadcast · ~15% system-broadcast · ~10% compliance-single + 1 promo-segment exemplar) · uz/ru/en hand-authored sample copy ("Pul jo'natildi — 5 000 000 UZS / 3 562 CNY", "Сниженные тарифы", "MyID expiring in 14 days") · UzCard/Humo only per LESSON 2026-04-30 — zero Visa/Mastercard mentions · 5-actor admin pool (Yulduz Otaboeva super · Adel Ortiqova finance · Bobur Yusupov comms · Madina Kholmatova ops/compliance · synthetic "System Notifications" actor for transactional per-user pushes) · **Schema cascade** `docs/models.md` §7 + §9.1 + §9.2: extended `NOTIFICATIONS` with 11 composer-side fields (`status` enum `scheduled / sending / sent / cancelled / failed` · `audience_type` enum `broadcast / segment / single` · `audience_criteria` jsonb · `scheduled_for` · `cancelled_at` · `cancellation_reason` text ≥20 chars · `composed_by` FK admin_users · `recipient_count` snapshot · `delivered_count` / `opened_count` / `click_through_count` nullable until status=sent); `user_id NULL = broadcast/segment` semantics preserved verbatim; `is_read` / `read_at` clarified as per-user-only (audience_type=single); 4 new indexes for cron pickup + admin-side listing + per-admin audit + per-user inbox · new state machine doc `docs/mermaid_schemas/notification_send_state_machine.md` (terminal `sent / failed / cancelled` immutable per "push notifications cannot be retracted") · `lib/deepLinkScreens.ts` extended to 10 screens (added `card_detail` + `settings`); new `DEEP_LINK_REQUIRED_PARAMS` map for soft-validation hints · PRD §6.1 row 13 added with Gherkin AC fragment · **List page** at `/content/notifications`: page header w/ Title + count subtitle (`{sent} sent · {scheduled} scheduled · {cancelled} cancelled`) + `+ New notification` brand-primary CTA on the right · carded filter bar (Phase 18b LESSON: `bg-card` outer + `bg-background` search input + chip row underneath) — debounced 300ms title search across uz/ru/en + type multi-chip + audience-type multi-chip + `<DateRangePicker>` for `sent_at` · sortable desktop table (non-sticky thead per LESSON 2026-04-30, Title-Case headers per LESSON 2026-05-02): Sent at relative+absolute / Type icon+label / `<AudienceCell>` (broadcast Megaphone+count / segment Filter+count / single avatar+name) / Title admin-locale / Read rate progress bar (em-dash for single / pre-send) / Sent by avatar / Status chip · mobile card stack mirror · click row → detail · 2-variant `<EmptyState>` (no-records → CTA · no-matches → Clear-all) · `j/k/Enter` row focus + `/` focus search + `f` focus first chip + `n` opens compose page (matches News' `n`) · 350ms initial-mount skeleton on subtitle counts · **Compose page** at `/content/notifications/new`: inline header w/ canonical `<ArrowLeft> Back to notifications` (no `← ` prefix per LESSON 2026-05-02; flow inline NEVER sticky per LESSON 2026-05-02) + `New notification` title + subtitle, then `<ComposePane>` consuming the full viewport · 60/40 two-col on `lg+` (form left, sticky `<PreviewPane>` right at `lg:top-4`); `<lg` stacks form + opens `<MobilePreviewSheet>` via Preview button · 6 form sections: AudienceSection (radio cards: All users / Filtered segment / Single user; conditional `<SegmentBuilder>` w/ tier multi · language multi · has-card tristate · has-transfer tristate · last-login enum 4-bucket; conditional `<UserPicker>` debounced 300ms name-or-phone search w/ avatar+tier-badge results; live `Estimated audience: N users` line via pure-function `estimateAudience(criteria)` against `mockUsers.ts` 50-user seed) → TypePicker (4-radio with per-option Tooltip; compliance tooltip explicitly notes "bypasses user notification preferences") → ContentSection (lifted `LocaleTabInputs` for title max 60 chars + lifted `LocaleTabTextarea` for body max 180 chars w/ hard-clip in onChange so the counter can't run past; per-locale red-dot when missing after `setShowErrors(true)` per News pattern) → DeepLinkSection (Switch toggle + screen `<Select>` 10 options + `<ParamsEditor>` JSON textarea w/ on-blur validation + soft-warning when required params missing per `DEEP_LINK_REQUIRED_PARAMS` + live preview-string `zhipay://<screen>?<query>`; ParamsEditor copied into `compose/` rather than lifted to `zhipay/` — defer lift until 3rd consumer) → ScheduleSection (radio cards Now / Later; conditional `<DateTimeInput>` w/ 5min granularity, `min = now+5min` snapped, error state when missing or in the past) · **Live preview** = `<PreviewPane>` w/ mode toggle (Lock screen / In-app), locale switcher (uz/ru/en using shared `<LocaleFlag>` + locale label keys matching LocaleTabInputs pattern), renders `<LockScreenPreview>` (gradient dark wallpaper + 9:41 clock + brand-mini-icon notification card with bold title + body + "now" + chevron+screen-name when deep-link) or `<InAppPreview>` (faux app header + ghost row + active row with type-toned icon chip + brand-tinted ring + chevron+screen-name) inside lifted `<PhoneMockup>` 9:16 frame · sticky-bottom `<ComposeActionBar>` uses canonical `fixed inset-x-0 bottom-0 md:left-[var(--sidebar-width,4rem)]` (LESSON 2026-05-02) + page wrapper `pb-28`; renders audience summary chip + type chip + schedule chip + Cancel + primary CTA (Send / Schedule per radio state) · **`<SendConfirmDialog>`** is one AlertDialog with 4 visual variants: standard send · large broadcast >5000 audience (additional warning banner) · compliance type (banner + typed-confirm input gates the primary CTA, trim+lowercase comparison against the localized "I confirm" / "Подтверждаю" / "Tasdiqlayman" string) · scheduled-for (title swaps to "Schedule for {time}?") · validation pattern: `summarizeMissing()` helper + always-clickable primary button + sonner toast naming missing fields (Phase 18b LESSON pattern) · `Cmd/Ctrl+1/2/3` swap locale tabs + `Cmd/Ctrl+Enter` submits with the same validation gate · Cancel button on action bar navigates back to list; submit (send/schedule) navigates to the new row's detail page · **Sent detail page** at `/content/notifications/sent/:id` — single-column outer w/ inline `DetailHeader` (back-link `<ArrowLeft> Back to notifications`; flow inline NEVER sticky; type chip + StatusChip + audience summary + sent/scheduled/cancelled timestamp) · two-column body on `lg+` (left col = `ContentCard` 3-locale read-only tabs (brand-tinted active state per Phase 18b styling) + `AudienceCard` (type + recipient count + criteria summary for segment / user link for single, deep-links to `/customers/users/:id`) + `DeepLinkCard` when applicable + `AuditCard` (composed-by `<UserAvatar>` + name + admin id; created/sent/scheduled/cancelled timestamps; cancellation reason banner warning-tinted when cancelled; "View in audit log" button → `/compliance/audit-log?entityType=notification&entityId={id}`); right col = `StatsCard` (Total sent · Delivered % w/ progress · Opened % w/ progress · Click-through % when deep_link present · 3-segment horizontal stacked bar for recipient language breakdown uz/ru/en computed via `audienceLanguageBreakdown(criteria)` against `mockUsers.preferred_language`)) stacks on `<lg` · `CancelScheduledActionBar` (sticky-bottom only when `status='scheduled'`) + `CancelScheduledDialog` (Dialog w/ ≥20-char reason + warning banner) → on confirm calls `cancelScheduledNotification(id, reason, actor)` mutator → status flips to `cancelled`, audit row written, toast.success · already-sent notifications have no admin actions per spec ("push notifications cannot be retracted") · **Mutators** all bridged via `bridgeNotificationAudit` → central `mockAuditLog` w/ `entity_type='notification'` (already in `AuditEntityType` enum pre-existing — no schema change needed there): `createNotification` (handles send-now → status=sent + simulated stats, and schedule-later → status=scheduled + null stats; emits one audit row w/ verb `send` or `schedule`) · `cancelScheduledNotification` (guards: id exists + status=scheduled + reason≥20; emits one audit row w/ verb `cancel_scheduled`); action map: send→`created`, schedule→`created`, cancel_scheduled→`status_changed` (preserves the scheduled→cancelled transition in `from_status`/`to_status`) · seed audit rows (one per existing notification, two for cancelled rows so the schedule + cancel transitions both appear) populated via IIFE so the audit-log surface has matching entries from session boot · **Routing**: `/content/notifications` + `/content/notifications/new` + `/content/notifications/sent/:id`; back-compat `/notifications` → `/content/notifications` via `RedirectPreservingQuery`; placeholder retired (PLACEHOLDER_ROUTES now empty); sidebar entry repointed; `g+i` chord listed in HelpOverlay + CommandPalette (cosmetic — the dashboard's chord prefix-handler is documentation-only at this point) · **i18n** ~150 new `admin.notifications.*` keys + 4 new `admin.deep-link.*` keys for the 2 new screens (`card-detail`, `settings`) |

### Cross-cutting open items

| Item | Status | Notes |
|---|:---:|---|
| Real brand assets (UzCard / Humo / Alipay / WeChat / ZhiPay logos) | ❌ | Stylized SVG placeholders today |
| Visa / Mastercard re-introduction in mock | ❌ | Paused; gated on explicit user instruction. See [LESSONS](../ai_context/LESSONS.md) 2026-04-30 |
| Senior-role wiring for AML / KYC escalate | 🚧 | Audit-log entry only; real role gate pending |
| Bulk reject (KYC) / bulk clear+escalate (AML) | ❌ | Single-row pattern covers common path |

---

## Mobile app (end-user)

Tech stack not chosen. **Implementation surface = Figma** ([the ZhiPay Figma file](https://www.figma.com/design/p8pxXYApMNCpzQn0XkoIw9/ZhiPay---v0.1--Project-) — official Figma MCP wired 2026-05-04, full read+write access verified). **Mobile design-system foundation LOCKED in Figma 2026-05-04** — Tokens (5 collections / 72 variables · 13 Text Styles · 8 Effect Styles), **Primitives layer COMPLETE** (5 of 5 — Button + Chip × 3 + Input × 3 + Avatar × 2 + Icon library), **Components layer COMPLETE** (11 of 11 — Card · Banner · Headline Number · List Row · Toast · Segmented Control · Tab Bar · Sheet · Modal · Status Timeline · Scheme Logo). **20 component sets** + **19 individual lucide icon components** live on the `❖ Components` page. All 16 spec MDs in [`mobile/design/primitives/`](../mobile/design/primitives/) (5 files) and [`mobile/design/components/`](../mobile/design/components/) (11 files) carry their `## Figma component-set` sections per the canonical rhythm (Variant axes → Naming → Variable bindings → File placement → Deviations table). Design-brief library complete: foundation brief + 9 user-flow plans + 11 surface prompts. **Next passes**: Patterns → Screens → Flows, following the layer hierarchy and the marquee path (Onboarding → MyID → Home → Card linking → Send-money → ...). Optional follow-ups: sample app shell (light / dark / 200% dynamic-type), Input Text/TextArea/Phone leading-trailing icon slots for full Button-parity.

### Foundation

| Layer | Status | Notes |
|---|:---:|---|
| Mobile design workspace ([`mobile/`](../mobile/)) | 🚧 | Folder + README + `research/references.md` (Wise + Apple + Behance aesthetic anchor) + `prompts/00-shared-context.md` (canonical paste-in: PRD recap, money/FX rules, status states, tier system, error codes, privacy invariants, brand tokens, forbidden patterns) + `prompts/01-foundation.md` (tokens / primitives / components / sample shell brief) + **9 of 9 user-flow plans** (onboarding · MyID · card linking · send-money · history · tier upgrade · card management · notifications · settings/support) + **11 of 11 surface prompts** (02 onboarding · 03 home · 04 card linking · 05 send-money · 06 history · 07 receipt · 08 tier-upgrade-and-MyID · 09 card mgmt · 10 notifications · 11 settings · 12 help-support). **Sequence locked**: foundation first, then surfaces in marquee-path order. Each prompt mirrors the locked template shape (goal · prerequisites · state-machine ref · per-screen layout + states · microinteractions · cross-screen patterns · ~30–80 i18n keys · longest-translation test · accessibility focus order · output checklist · forbidden patterns · cross-references). References canonical truth (mermaid schemas + `.claude/rules/`), never duplicates. **Tokens layer locked 2026-05-04** in [`mobile/design/tokens/`](../mobile/design/tokens/); subsequent layers (Primitives / Components / Patterns / Screens / Flows) pending |
| Tech stack decision (RN / Flutter / native iOS+Android) | ❌ | Open — see [`AI_CONTEXT.md`](../ai_context/AI_CONTEXT.md) "Decisions made" |
| Design tokens (mobile-first) | ✅ | **Phase 26 (2026-05-04): foundation now LIVE in Figma** — 5 Variable collections (Primitives 33 color vars w/ single "Value" mode · Color 19 semantic aliases w/ Light + Dark modes · Spacing 13 · Radius 4 · Duration 3 = 72 vars total) + 13 Text Styles + 8 Effect Styles published in [the file](https://www.figma.com/design/p8pxXYApMNCpzQn0XkoIw9/ZhiPay---v0.1--Project-). **Architecture refactored mid-build (user-driven)**: Primitives split out of Color into a separate collection; semantic vars renamed `color/semantic/{role}` → `color/{role}` (the "Color" collection IS the semantic layer; prefix was redundant); `color/base/white` + `color/base/black` added so every semantic mode-value resolves through a primitive — zero raw colors in the semantic layer. **Markdown spec docs** at 10 files in [`mobile/design/tokens/`](../mobile/design/tokens/): [`figma-setup.md`](../mobile/design/tokens/figma-setup.md) (canonical setup guide, updated this cascade w/ Primitives/Color split + base family + 13 Text Styles list + new gradient-can't-bind-color-vars limitation) · [`colors.md`](../mobile/design/tokens/colors.md) (11-stop brand + slate, 3-stop semantic, semantic mappings light + dark, full WCAG contrast table) · [`typography.md`](../mobile/design/tokens/typography.md) (6-role scale 44/32/22/16/14/13 — Display 1 / 2 / Heading / Body / Body small / Label; mobile lifts display sizes higher than admin's web scale; tabular-nums on every monetary/count/ID/timestamp value; dynamic-type to 200%) · [`spacing.md`](../mobile/design/tokens/spacing.md) (8pt base, `space-0`…`space-12`, page-gutter + component-padding presets, ≥44pt tap rule) · [`radii.md`](../mobile/design/tokens/radii.md) (4 stops + pill: 8/12/20pt + 9999px) · [`shadows.md`](../mobile/design/tokens/shadows.md) (3 elevations × 2 modes + brand-tinted `shadow-hero` for home card-as-object + receipt) · [`motion.md`](../mobile/design/tokens/motion.md) (duration/easing tokens · default 220ms vs admin's 180ms · 8 reserved animated patterns · reduced-motion fallback) + [`tokens.json`](../mobile/design/tokens/tokens.json) (Tokens Studio / Style Dictionary export — optional Figma sync path) + [`tailwind.tokens.css`](../mobile/design/tokens/tailwind.tokens.css) (CSS vars + paste-into Tailwind config snippet — downstream code consumers) + [`README.md`](../mobile/design/tokens/README.md) (layer index + consumption rules + mobile-vs-admin deviations + canonical Figma path pointer). **Inherits brand + slate + semantic palettes verbatim from admin dashboard** for cross-surface parity (`brand-600 = #0a64bc`). **Mobile-specific deviations** (intentional, documented): `radius-sm` 4→8pt, `radius-lg` 12→20pt, `shadow-hero` (new), `duration-base` 180→220ms, Display 1 +8pt, Display 2 +4pt, Body 15→16pt |
| Mobile primitives | ✅ | **LOCKED 2026-05-04**. All 5 primitives built in Figma + their `## Figma component-set` spec sections appended. **Button** (`56:38`, 14 cells = Variant × Size; INSTANCE_SWAP icon slots) · **Chip** ([3 sets](../mobile/design/primitives/chip.md): `Chip / Status` 30 cells · `Chip / Tier` 9 cells · `Chip / Count` 18 cells = **57 cells**; `Tier2` MyID glyph stand-in until `Icon / BadgeCheck` lands) · **Input** ([3 sets](../mobile/design/primitives/input.md): `Input / Field` 12 cells × State + INSTANCE_SWAP for Search leading/trailing icons · `Input / Otp` 6 cells × Length+State · `Input / NumberPad` 2 cells × Locale w/ INSTANCE_SWAP Delete · = **20 cells**; PIN deferred per spec) · **Avatar** ([2 sets](../mobile/design/primitives/avatar.md): `Avatar / Initials` 15 cells × Color+Size · `Avatar / Photo` 3 cells × Size = **18 cells**; status-dot + selected-ring deferred) · **Icon** (no component set — [each lucide icon is its own COMPONENT](../mobile/design/primitives/icon.md), now **19 icons total** in the library: 13 from Phase 26 + 4 from Pass 2 Input (Search · X · Delete · Lock) + 2 from Pass 2 Tab Bar (Home · Menu); `Icons (17 — full library)` showcase frame at (100, 1900)). **Total Primitives cells: 122** + 19 icon components. **Page-context lesson** captured Phase 27c+: always `await figma.setCurrentPageAsync(componentsPage)` before component creation (chip Pass 2 hit this pitfall, fixed via reparent). Spec-vs-build deviations all tracked with documented recovery paths. |
| Mobile components | ✅ | **LOCKED 2026-05-04**. All 11 components built in Figma + their `## Figma component-set` spec sections appended. From **Phase 26**: [`Card`](../mobile/design/components/card.md) (6 variants incl. `Card as Object` w/ brand gradient + shadow-hero) · [`Banner`](../mobile/design/components/banner.md) (4 tones, light-only) · [`Headline Number`](../mobile/design/components/headline-number.md) (Display 44 / Hero 32 / Inline 16) · [`List Row`](../mobile/design/components/list-row.md) (5 variants — Avatar/Switch/Radio now real instances since primitives shipped). From **Phase 27d (this sweep)**: [`Toast`](../mobile/design/components/toast.md) (4 tonal cells, 3pt left accent, shadow-md) · [`Segmented control`](../mobile/design/components/segmented-control.md) (6 cells = Variant × Size) · [`Tab bar`](../mobile/design/components/tab-bar.md) (4 ActiveTab cells) · [`Sheet`](../mobile/design/components/sheet.md) (3 SnapPoint cells: Peek / Half / Full) · [`Modal`](../mobile/design/components/modal.md) (3 Variant cells: Confirm / Destructive / Info) · [`Status timeline`](../mobile/design/components/status-timeline.md) (4 Variant cells across terminal states) · [`Scheme logo`](../mobile/design/components/scheme-logo.md) (12 cells = Scheme × Size; Visa/MC excluded per LESSON 2026-04-30, registered in spec). **Total Components cells: ~70** across 11 sets. **2 build pitfalls captured** (both `resize(_, 0)` + `primaryAxisSizingMode='AUTO'` not recomputing → force `layoutSizingVertical='HUG'` explicitly): Modal CTA clipping, Status Timeline cells collapsing to 0pt height. **Open follow-ups (none blocking)**: replace Scheme Logo placeholder marks with licensed brand SVG/PNG when they land; Tab Bar CountBadge slot via INSTANCE_SWAP when notifications need it; Smart Animate prototype connections for sheet snap / modal open / timeline pulse (animation territory). |
| Mobile patterns | 🚧 | **Phase 28 (2026-05-04): Pattern 1 — FX Quote Breakdown drafted in Figma** on the `⏰ Working files` page as 6 state cells (Default · Loading · Error · Offline · Stale · Rate-moved). Composes Card (16pt radius, shadow-sm) + Headline Number (Hero 32pt brand-600 for recipient-gets) + Inline Row (you-send / fee+sublabel / fx-spread / total bold) + Rate row + Locked / Stale chip + countdown ("Locked for 02:34") + Banner (info / warning / danger) per state. Translated from [`mobile/ZhiPay/pattern-fx-quote.jsx`](../mobile/ZhiPay/pattern-fx-quote.jsx). **Remaining patterns ❌**: KYC step · recipient picker · send-money review block · receipt block · transfer-status detail · card-link 3DS wrapper. See [design-system-layers](../.claude/rules/design-system-layers.md). |
| Mobile screens — Claude Design exploration ([`⏰ Working files`](https://www.figma.com/design/p8pxXYApMNCpzQn0XkoIw9/ZhiPay---v0.1--Project-?node-id=1-5)) | 🚧 | **Phase 28 (2026-05-04)**: 17 reference frames + 6 pattern cells = 23 frames pixel-perfect-translated from Claude Design's React/JSX output ([`mobile/ZhiPay/`](../mobile/ZhiPay/)) onto iPhone-15-Pro device frames (402×874). Layout: 4 horizontal rows w/ section labels — **Auth** (y=200, 7 frames) · **Home** (y=1324, 4 frames) · **Transfer** (y=2448, 6 frames) · **Pattern 1** (y=3622+y=4282, 6 cells). Light mode · uz copy. **NOT canonical screens** — these are visual references / drafts; the per-surface rows below stay ❌ until each ships a full spec MD with all 6 generic states + tier_0/1/2 variants + i18n keys + acceptance criteria + state-machine refs. **Phase 29 (2026-05-04)**: all 17 reference frames refactored from inline geometry to component instances from `❖ Components` — full swap log: **Auth row** (Splash: ZhiMark + Button Primary Lg · Phone: Auth top bar Step=1 + Input/Field Phone + Button · OTP: Auth top bar Step=2 + Input/Otp Six + Numpad · PIN-Create: Auth top bar Step=3 + Numpad · PIN-Confirm: Auth top bar Step=4 + Numpad · Onboarding: Button · Lock: ZhiMark + Numpad) · **Home row** (Safe: Tab bar Home + Banner Info · Bold: Tab bar Home + Banner Info · Profile: Avatar/Initials Lg + Tab bar More · Cards manage: Scheme logo Sm UzCard + Humo) · **Transfer row** (Recipient select: Input/Field Search + 4× Avatar/Initials Md (Brand/Warning/Success/Slate) · Add recipient: 3× Input/Field Text + Button · Amount entry: Button · Review: Button · Processing: skipped — custom spinner · Success: Avatar/Initials Md + Button Primary Lg + Button Secondary Lg). 2 new component sets authored to fill gaps: **Auth top bar** (`170:265`, 4 variants Step=1|2|3|4) and **Brand/ZhiMark** (`170:223`, single component). iOS chrome (status bar / dynamic island / home indicator) stayed inline (library Status Bar is 375w; frames are 402w). Pattern 1 cells skipped (already pattern-grade). New build pitfalls captured: (1) auto-layout components default to `primaryAxisSizingMode=AUTO` after `c.layoutMode=...` — must set FIXED + resize for stable widths; (2) Numpad's `AmountField` child can be hidden via `visible=false` to repurpose the component on OTP/PIN/Lock without the currency headline; (3) `safe(node, 'children')` traversal needed for VECTOR/RECT nodes when finding text inside instances. **Phase 29 follow-up — code-vs-Figma parity**: User flagged "no all pages in figma that exists in code." `OnboardingScreen` ([`auth-screens.jsx:340`](../mobile/ZhiPay/auth-screens.jsx) `ONBOARDING_SLIDES`) defines 3 slides; only slide 1 was in Figma. Added **slide 2** (`189:345` at x=3754, "Aniq kurs" + lock-and-chart 180×180 illustration + page-indicator dot at index 1) and **slide 3** (`189:389` at x=4276, "Xavfsiz va litsenziyalangan" + shield-and-check illustration + page-indicator dot at index 2) by cloning slide 1 and rebuilding illustration vector + title/body + indicator per slide. Slide 3 title required `textAutoResize=HEIGHT` + width=322 to wrap. **Frame count: 23 → 25.** Other code-vs-Figma deltas (Home empty states · KYC tier banner variants · Lock+FaceID variant · Profile tier-specific row visibility) are state branches of existing screens — deferred to canonical screen-spec passes. **Phase 29 follow-up #2 — inline icons → instances**: User asked to "remake the do not component instance icons in working files." Audited every frame, found 45 inline VECTOR icons. **30 swapped** to library icon instances: 14× ChevronRight (list-row trailing arrows on Profile + Recipient select + Add recipient + Home B CTA) · 5× ChevronLeft (back arrows on Cards manage + Recipient select + Add recipient + Amount entry + Review) · 4× User/CreditCard/ShieldCheck/Info (Profile leading icons) · 2× Send (Home A CTA + Processing spinner) · 1× ArrowRight (Home A CTA) · 1× Search (Recipient select search bar) · 1× Lock (Review rate-lock chip, sized down to 14×14) · 1× Check (Success indicator) · 3× Plus (Home A "Qo'shish" pill + Cards manage "Karta qo'shish" + Recipient select "Yangi oluvchi qo'shish", brand-600 stroke override). 2 new icon components authored to fill gaps: **Icon / ChevronLeft** (`196:441`) and **Icon / Plus** (`198:451`). Library icon count: **19 → 21 lucide icons**. **13 inline vectors deliberately not swapped** (no library counterpart, deferred): ~~Bell (Home A header + Profile notifications row, 2×)~~ — added in Phase 29 follow-up #4 below · Globe (Profile language row) · Question/HelpCircle (Profile help row) · Logout/LogOut (Profile sign-out row) · Star (Cards manage default-card indicator) · Trash (Cards manage row destructive action, 2×) · Settings/three-dots (Cards manage row overflow) · Up-down swap arrows (Amount entry currency swap) · 6 illustration vectors on Onboarding slides 1/2/3 (artwork, not icons). One misclassification caught + corrected mid-pass: 156:27 was the "+" Plus on the Add-recipient row, mistakenly mapped to Search; reverted by removing the Search instance + replacing with the new Plus instance. **Phase 29 follow-up #3 — Onboarding illustrations → component set**: User asked to make onboarding illustrations also reusable instances. Authored **Onboarding illustration** component set (`204:924`, key `27bdf4e31a...`, 3 variants `Slide=1|2|3` × 180×180) on the Components page. Each variant: brand-50 outer ellipse 148×148 at (16,16) + slide-specific main shape — Slide 1 brand-100 inner ellipse 100×100 + brand-600 bolt vector centered; Slide 2 brand-600 lock body 60×48 + brand-700 shackle U-vector positioned correctly above body (78,48) + white keyhole dot+stem; Slide 3 brand-600 shield 60×82 centered + white 5pt-stroke check inside. Swapped all 3 onboarding slides' inline illustration frames (`144:17` · `189:360` · `189:404`) with the corresponding variant instances — also fixes the original Slide 2/3 bug where the shackle/shield/check vectors were positioned at (0,0) instead of inside the circle. Components count on `❖ Components` page: **22 → 23 component sets**. **Phase 29 follow-up #4 — Icon / Bell authored + bells swapped**: User asked "make bell icon instance." Authored **Icon / Bell** (`209:1140`, key `ddf3bb6cb8...`, 24×24, lucide bell shape — body cubic Bezier `M 6 8 C 6 4.687 8.687 2 12 2 C 15.313 2 18 4.687 18 8 C 18 15 21 17 21 17 L 3 17 C 3 17 6 15 6 8 Z` + small clapper `M 10 21 Q 12 23 14 21`, both 2pt foreground stroke ROUND). Cubic Bezier used over Q-curves for accurate half-circle dome. Swapped both inline bell vectors to instances: **153:20** (Home A top-right header) → instance `209:1514` · **154:43** (Profile Bildirishnomalar row leading icon) → instance `209:1517`. Library icon count: **21 → 22 lucide icons**. **Phase 29 follow-up #5 — Icon / Inbox authored**: User asked for an empty-box icon for empty states. Authored **Icon / Inbox** (`219:1243`, key `9256dacb2a...`, 24×24, lucide-inbox shape — single combined path: tray "U-dip" notch (`M 22 12 L 16 12 L 14 15 L 10 15 L 8 12 L 2 12`) + outer rounded container with cubic-Bezier corner approximations (`a 2 2` arcs converted using magic-constant 0.5523 control points), 2pt foreground stroke ROUND). Initial attempt was an `Illustration / Empty state` 180×180 component but user clarified the request was for an icon, not an illustration — the illustration was deleted before the doc-cascade landed. Library icon count: **22 → 23 lucide icons**. Available for use on empty-state list rows / empty history / empty saved-recipients / empty notifications screens. |
| Localization seed (`i18n/{uz,ru,en}.json`) | ❌ | UZ default · RU + EN co-equal · per-screen i18n keys enumerated in surface prompts; JSON files generate alongside the rendered designs |
| Real brand assets (logo · scheme marks · destination marks) | ❌ | Same gap as dashboard |

### Surfaces

| Surface | Status | Notes |
|---|:---:|---|
| Onboarding (welcome · language picker · phone OTP · tier_1 landing) | ❌ | Brief: [`02-onboarding-screens.md`](../mobile/prompts/surfaces/02-onboarding-screens.md) (paired with [`flow-01-onboarding.md`](../mobile/prompts/user-flows/flow-01-onboarding.md)). First-run only. Rendered output pending |
| Home (tier_1 view-only · tier_2 full · tier_2-expired soft-demote) | ❌ | Brief: [`03-home-screen.md`](../mobile/prompts/surfaces/03-home-screen.md). Mobile primary surface; banner triggers tied to [`flow-06-tier-upgrade.md`](../mobile/prompts/user-flows/flow-06-tier-upgrade.md) |
| Card linking (UzCard / Humo; Visa/MC `tier_2`-gated) | ❌ | Brief: [`04-card-linking-screens.md`](../mobile/prompts/surfaces/04-card-linking-screens.md) (paired with [`flow-03-card-linking.md`](../mobile/prompts/user-flows/flow-03-card-linking.md)). 3DS WebView · see [card-schemes](../.claude/rules/card-schemes.md) |
| Send money (recipient → amount → FX review → 3DS → submitting → failure) | ❌ | Brief: [`05-send-money-screens.md`](../mobile/prompts/surfaces/05-send-money-screens.md) (paired with [`flow-04-send-money.md`](../mobile/prompts/user-flows/flow-04-send-money.md)). Rate-lock + breakdown per [money-and-fx](../.claude/rules/money-and-fx.md) |
| Transfer history (list · filter sheet · detail) | ❌ | Brief: [`06-history-screens.md`](../mobile/prompts/surfaces/06-history-screens.md) (paired with [`flow-05-history.md`](../mobile/prompts/user-flows/flow-05-history.md)). Mirrors `transfers` table with locale formatting |
| Receipt (post-send + history detail; shared component) | ❌ | Brief: [`07-receipt-screens.md`](../mobile/prompts/surfaces/07-receipt-screens.md). Timeline from [`transfer_state_machine.md`](./mermaid_schemas/transfer_state_machine.md). Re-mounted by History detail |
| Tier-upgrade prompts + MyID verification (consent · ID · face · result) | ❌ | Brief: [`08-tier-upgrade-screens.md`](../mobile/prompts/surfaces/08-tier-upgrade-screens.md) (paired with [`flow-02-myid.md`](../mobile/prompts/user-flows/flow-02-myid.md) + [`flow-06-tier-upgrade.md`](../mobile/prompts/user-flows/flow-06-tier-upgrade.md)). Banner / sheet / full-screen prompts + the MyID flow itself |
| Card management (list · detail · freeze · unfreeze · remove) | ❌ | Brief: [`09-card-management-screens.md`](../mobile/prompts/surfaces/09-card-management-screens.md) (paired with [`flow-07-card-management.md`](../mobile/prompts/user-flows/flow-07-card-management.md)). Status mapping per [card-schemes](../.claude/rules/card-schemes.md) |
| Notifications inbox + push handling | ❌ | Brief: [`10-notifications-screens.md`](../mobile/prompts/surfaces/10-notifications-screens.md) (paired with [`flow-08-notifications.md`](../mobile/prompts/user-flows/flow-08-notifications.md)). Inbox + filter sheet + detail + foreground push toast; `type ∈ {transfer, compliance, security, card, system, marketing}` |
| Settings (profile · KYC · language · notif prefs · appearance · security · sign-in history · about · sign-out · account deletion) | ❌ | Brief: [`11-settings-screens.md`](../mobile/prompts/surfaces/11-settings-screens.md) (paired with [`flow-09-settings.md`](../mobile/prompts/user-flows/flow-09-settings.md)). `users.preferred_language` drives all formatting |
| Help / Support (help center · article · contact form · confirmation) | ❌ | Brief: [`12-help-support-screens.md`](../mobile/prompts/surfaces/12-help-support-screens.md) (shares [`flow-09-settings.md`](../mobile/prompts/user-flows/flow-09-settings.md)) |

---

## Cross-references

- Authoritative facts → [`docs/models.md`](./models.md) · [`docs/product_requirements_document.md`](./product_requirements_document.md) · [`docs/mermaid_schemas/`](./mermaid_schemas/)
- Project state + decisions → [`ai_context/AI_CONTEXT.md`](../ai_context/AI_CONTEXT.md)
- Lessons → [`ai_context/LESSONS.md`](../ai_context/LESSONS.md)
- Change log → [`ai_context/HISTORY.md`](../ai_context/HISTORY.md)
- Workflow → [`CLAUDE.md`](../CLAUDE.md)
